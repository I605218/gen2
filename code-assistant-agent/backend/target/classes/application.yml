server:
  port: 8080

spring:
  application:
    name: code-assistant-agent
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql6.sqlpub.com:3311/codeassistanceagent?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${DB_USERNAME:zhongyixiao}
    password: ${DB_PASSWORD:CF897WOCURMgdikY}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always

ai:
  api:
    base-url: ${AI_API_BASE_URL:https://api-inference.modelscope.cn/v1}
    api-key: ${AI_API_KEY:}
    model: ${AI_API_MODEL:Qwen/Qwen3-235B-A22B-Instruct-2507}
    temperature: ${AI_API_TEMPERATURE:0.7}
    max-tokens: ${AI_API_MAX_TOKENS:1024}
    max-concurrent-requests: ${AI_API_MAX_CONCURRENT_REQUESTS:8}
    connect-timeout-millis: ${AI_API_CONNECT_TIMEOUT_MILLIS:10000}
    read-timeout-millis: ${AI_API_READ_TIMEOUT_MILLIS:120000}
