# Code Assistant Agent 应用设计文档

## 1. 文档说明

本文档面向 `迭代二/code-assistant-agent` 项目编写，综合参考并整合以下三份已有文档：

- `README.md`
- `接口文档.md`
- `CI_CD_部署全流程思路.md`

本文档的重点不是一般性的系统介绍，而是**围绕课程中提出的 Agent 基本要求与加分项，逐条说明本项目是如何落地实现的**。在此基础上，再补充系统架构、接口设计与部署设计，形成一份更适合课程答辩与项目提交的正式设计文档。

---

## 2. 项目概述

`Code Assistant Agent` 是一个面向编程学习场景的智能体应用。项目采用前后端分离架构，前端基于 `Vue 3 + Vite`，后端基于 `Spring Boot 3`，同时提供用户系统、历史记录、工作台统计、Agent 执行链路展示以及 Docker Compose + Nginx 的部署方案。

本项目的目标并不是简单封装一个大模型对话接口，而是构建一个具备如下特征的完整 Agent 应用：

1. 支持自然语言模式与结构化模式两种任务输入方式
2. 支持面向编程学习的多种任务类型
3. 支持规划、行动、反思、重规划等多阶段执行过程
4. 支持主题相关工具接入与工具轨迹展示
5. 支持用户登录态与游客态并存
6. 支持课程展示、联调测试和后续工程化扩展

---

## 3. 项目整体结构与架构

### 3.1 项目目录结构

```text
code-assistant-agent/
├── backend/              # Spring Boot 后端
├── frontend/             # Vue 3 + Vite 前端
├── deploy/               # Docker Compose / Nginx / 环境示例
├── scripts/              # 部署脚本
├── README.md
├── 接口文档.md
└── CI_CD_部署全流程思路.md
```

### 3.2 架构分层

系统逻辑上分为四层：

1. **表现层**
   - Vue 前端工作台
   - 负责输入任务、展示回答、展示历史记录和内部状态

2. **服务层**
   - Spring Boot 控制器与业务服务
   - 负责任务解析、Agent 执行、认证、工作台聚合

3. **智能体执行层**
   - 任务解析
   - 规划与执行
   - 工具调用
   - 反思与修正
   - 最终回答生成

4. **基础设施层**
   - MySQL / H2
   - OpenAI 兼容模型接口
   - Docker Compose
   - Nginx
   - Shell 部署脚本与 CI/CD 流程

### 3.3 整体交互流程

```text
用户在前端输入问题
   ↓
前端调用后端 Agent 接口
   ↓
后端解析自然语言请求或直接接收结构化请求
   ↓
Agent 执行：规划 → 工具调用 → 反思 → 重规划 → 生成回答
   ↓
后端保存历史记录、会话状态与执行结果
   ↓
前端展示最终回答、执行轨迹、会话状态和统计信息
```

---

## 4. 课程要求对应实现说明

本节是本文档的核心部分。以下内容将**按照课程提出的 Agent 基本要求与加分项逐条对应说明**，明确指出：

- 课程要求是什么
- 本项目如何实现
- 当前实现达到了什么程度
- 在系统中具体体现在哪里

---

## 4.1 基本需求点一：Agent 需要展现出规划和思考能力，而不是简单预定义 workflow

### 4.1.1 课程要求理解

这一要求强调，系统不能只是“接收用户输入 → 拼接 prompt → 调用模型 → 返回答案”的单层流程，而应当体现出：

- 对任务目标的理解
- 对执行过程的规划
- 对中间观察结果的利用
- 对结果不足时的修正能力

也就是说，系统需要体现“思考过程”，而不是简单预设流程的外壳。

### 4.1.2 本项目的落地实现

本项目在 Agent 主流程中，将一次执行拆分为以下显式阶段：

1. `planning`
2. `action`
3. `reflection`
4. `replanning`
5. `answer_generation`

这种设计意味着系统不是一次性直接输出答案，而是先构建计划，再执行工具调用，再做反思和必要修正，最后生成最终回答。

### 4.1.3 具体实现方式

在当前实现中，Agent 会在一次执行中构建和输出以下结构化信息：

- `planSteps`：计划步骤
- `reasoningSteps`：推理摘要
- `actionTraces`：行动轨迹
- `reflection`：反思结果
- `stageTraces`：阶段执行轨迹
- `memory`：执行过程中的关键记忆项

这些数据不是单纯用于展示，而是实际参与最终回答生成与修正过程。

### 4.1.4 为什么它不是简单 workflow

虽然本项目仍然属于课程项目中的工程化实现，而不是完全开放式自治 Agent，但它已经具备以下动态性：

- 不同任务类型会生成不同的任务目标
- 是否调用工具取决于任务类型匹配
- 是否触发二次修正取决于工具观察与反思结果
- 当工具信息不足时，会进入重规划并补强答案

因此，本项目并不是“固定模板直接吐答案”，而是具备“规划—行动—反思—修正”的显式思考结构。

### 4.1.5 当前达成程度说明

本项目已经满足“展现规划和思考能力”的基本要求，原因在于：

- 存在独立规划阶段
- 存在行动与观察阶段
- 存在反思与重规划阶段
- 存在结构化执行轨迹输出

如果后续继续增强，可进一步演进为：

- 由模型动态生成完整计划
- 按观察结果实时调整下一步动作
- 引入多轮决策循环

但从课程项目要求角度看，当前实现已经能够明确体现“不是简单 workflow”。

---

## 4.2 基本需求点二：Agent 需要具备与外部世界交互的能力，至少设计实现 2 个主题相关的外部工具

### 4.2.1 课程要求理解

这一要求强调，Agent 不能只依赖模型内部知识，还应当具备某种“行动能力”，即通过调用工具获取外部观察结果，并把这些观察纳入回答过程。

工具未必一定是联网搜索，也可以是与主题密切相关的外部能力模块。关键在于：

- 工具与项目主题相关
- 工具可被 Agent 调用
- 工具输出会参与后续推理与回答

### 4.2.2 本项目已实现的工具

本项目围绕“编程学习与代码辅助”主题，已实现 3 个工具：

1. `ErrorKeywordTool`
2. `CodeComplexityTool`
3. `LearningResourceTool`

这已经超过课程要求中“至少 2 个工具”的下限。

### 4.2.3 工具总体接入方式

在 `action` 阶段，Agent 会根据任务类型筛选可用工具，并执行最多 2 个工具。工具执行结果会进入：

- `toolResults`
- `actionTraces`
- `tools`
- `execution.memory`

说明工具结果不是孤立存在的，而是被纳入后续推理、反思和回答生成。

### 4.2.4 工具一：ErrorKeywordTool

**适用任务类型**

- `ERROR_EXPLANATION`
- `CODE_REVIEW`

**工具作用**

- 读取 `errorMessage` 或 `question`
- 检测常见错误模式
- 输出面向教学的诊断摘要

**典型支持场景**

- 空指针异常分析
- 越界错误分析
- 语法错误初步归因

**为什么满足课程要求**

这个工具直接面向编程学习中高频出现的报错理解问题，属于与项目主题高度相关的外部能力模块。

### 4.2.5 工具二：CodeComplexityTool

**适用任务类型**

- `CODE_REVIEW`
- `ALGORITHM_GUIDE`

**工具作用**

- 读取代码片段
- 统计代码行数
- 统计循环数量
- 输出复杂度与可读性相关摘要

**典型支持场景**

- 代码审查中的复杂度提醒
- 算法讲解中的实现结构分析
- 循环层级较多时的性能讨论

**为什么满足课程要求**

该工具直接服务于代码优化、复杂度分析和算法理解，属于编程学习主题下的核心工具。

### 4.2.6 工具三：LearningResourceTool

**适用任务类型**

- `ALGORITHM_GUIDE`
- `PRACTICE_GENERATION`
- `GENERAL_CHAT`

**工具作用**

- 根据问题或知识点生成学习路径建议
- 帮助 Agent 在回答中加入“下一步怎么学”

**典型支持场景**

- 算法讲解后的延伸学习建议
- 练习生成前后的学习路线补充
- 通用问答中的拓展学习推荐

**为什么满足课程要求**

该工具强化了本项目作为“编程学习工作台”的教育属性，使 Agent 不只是答题，还能给出学习路径。

### 4.2.7 工具与推理过程的协同关系

本项目中的工具不是简单地写几个工具类，而是被放入完整执行闭环中：

- `Plan-and-Execute` 决定需要什么样的观察信息
- `ReAct-style` 负责真正执行工具调用
- `Reflexion` 根据工具结果判断答案是否需要补强

因此，本项目已经明确满足“具备外部工具交互能力，并至少实现 2 个主题相关工具”的要求。

### 4.2.8 当前达成程度说明

课程要求：至少 2 个工具。  
本项目实现：3 个工具。  
课程要求：工具要服务主题场景。  
本项目实现：3 个工具都直接服务于编程学习与代码辅助。  
课程要求：Agent 要能利用工具。  
本项目实现：工具结果进入行动轨迹、反思过程与最终回答生成。

因此，这一项属于**明确达成且完成度较高**。

---

## 4.3 基本需求点三：Agent 需要具备自我修正能力，能够捕获系统中的异常并处理

### 4.3.1 课程要求理解

这一要求包含两个层次：

1. **Agent 层的自我修正能力**
   - 能发现当前回答可能不足
   - 能对结果进行补强、修正或重试

2. **系统层的异常处理能力**
   - 在运行异常时，系统不会直接崩溃
   - 能对异常做统一处理并返回受控错误信息

### 4.3.2 本项目的 Agent 层自我修正实现

本项目在 Agent 执行中显式设置了 `reflection` 阶段。该阶段会：

- 总结本轮工具观察是否充分
- 判断当前答案是否需要补强
- 在需要时触发 `replanning`
- 在回答生成后进一步进入 `refineAnswer`

### 4.3.3 reflexion 触发条件

当前系统会基于以下条件决定是否触发二次修正：

- 用户是否显式开启 `enableReflexion`
- 当前是否没有拿到工具观察结果
- 当前反思内容中是否提示需要“重规划”或“进一步补强”

### 4.3.4 自我修正的执行方式

当 reflexion 被触发后，系统会：

1. 增加 `retryCount`
2. 重建计划步骤
3. 重建推理摘要
4. 在 `memory` 中记录重规划原因
5. 再次调用模型生成更完善的答案

这说明系统不是简单重复调用模型，而是带有“反思依据 + 重规划逻辑”的修正过程。

### 4.3.5 系统层异常处理实现

除了 Agent 层反思修正外，后端还实现了系统异常处理能力，覆盖例如：

- AI 请求繁忙
- AI 接口调用失败
- 登录状态异常
- 业务校验异常
- 全局异常统一返回

这样可以保证：

- 后端发生异常时不直接崩溃
- 前端能拿到受控错误消息
- 用户可以感知失败原因并继续操作

### 4.3.6 当前达成程度说明

从课程要求角度看，本项目已经满足该项要求：

- Agent 层面：有反思、自我修正、重规划机制
- 系统层面：有统一异常处理机制

当前版本更偏“基础可解释实现”，后续可继续增强：

- 工具失败后的自动替代策略
- 多轮重试与降级策略
- 熔断与限流
- 更细粒度的日志、告警和异常分类

但就课程要求而言，本项目已经**明确达标**。

---

## 4.4 基本需求点四：Agent 需要可视化内部状态流转情况（CLI、Web 等均可）

### 4.4.1 课程要求理解

这一要求强调，Agent 不应只展示最终答案，还应展示内部状态流转过程，让使用者能够理解：

- 当前执行了哪些阶段
- 每个阶段做了什么
- 是否调用了工具
- 是否进行了反思与修正

### 4.4.2 本项目的可视化方式

本项目采用 **Web 工作台** 作为主要可视化载体，后端返回结构化执行数据，前端负责承载和展示。

### 4.4.3 当前已实现的内部状态可视化内容

后端在 `AgentResponse` 中已经返回以下字段：

- `frameworks`
- `execution`
- `planSteps`
- `reasoningSteps`
- `actionTraces`
- `tools`
- `reflection`
- `answer`

这些字段足以展示一次 Agent 执行从开始到结束的主要状态流转。

### 4.4.4 execution 元信息可视化

`execution` 中进一步包含：

- `executionId`
- `planStepCount`
- `toolCallCount`
- `actionTraceCount`
- `reflexionTriggered`
- `retryCount`
- `totalDurationMs`
- `memory`
- `stageTraces`

说明系统不只是展示静态文字，而是输出了结构化执行元数据。

### 4.4.5 分阶段状态流转可视化

当前系统将执行过程拆分为以下阶段：

- `planning`
- `action`
- `reflection`
- `replanning`
- `answer_generation`

每个阶段记录：

- `stage`
- `framework`
- `status`
- `attempt`
- `durationMs`
- `summary`

这意味着前端可以展示：

- 当前属于哪个阶段
- 使用了什么推理框架
- 阶段是否成功完成
- 花费了多少时间
- 阶段摘要是什么

### 4.4.6 细粒度执行过程可视化

#### 1）计划层可视化

通过 `planSteps` 展示：

- 计划共几步
- 每一步的目标是什么
- 属于哪种框架
- 当前状态如何

#### 2）推理摘要可视化

通过 `reasoningSteps` 展示：

- 识别出的任务类型
- 用户问题摘要
- 是否接收了代码上下文
- 是否接收了错误信息
- 是否显式开启 reflexion

#### 3）行动轨迹可视化

通过 `actionTraces` 展示：

- Thought
- Action
- Observation

这是系统中最接近 ReAct 风格解释链路的可视化部分。

#### 4）工具调用可视化

通过 `tools` 展示：

- 调用了哪个工具
- 工具输入是什么
- 工具输出是什么

#### 5）反思结果可视化

通过 `reflection` 和 `execution.reflexionTriggered` 展示：

- 是否进行了自我检查
- 是否认为答案还需补强
- 是否进入二次修正或重规划

#### 6）执行记忆可视化

通过 `memory` 展示：

- 当前任务类型
- 用户问题摘要
- 初始计划步数
- 工具观察数量
- reflexion 判定结果
- retry 原因
- 最终答案是否生成

### 4.4.7 当前达成程度说明

课程要求允许使用 CLI 或 Web。  
本项目采用 Web 工作台完成可视化。  
课程要求希望能展示内部状态流转。  
本项目已经展示：计划、推理摘要、行动轨迹、工具调用、反思结果、执行元信息、阶段轨迹。  

因此，这一项属于**明确达成且具有较强展示效果**。

---

## 4.5 加分项：Agent 有机结合不止一种推理框架，如 ReAct、Plan-and-Execute、Reflexion 等

### 4.5.1 课程要求理解

这一加分项强调，系统不应只使用单一推理方式，而应将多种 Agent 思想融合在同一条执行链路中，让不同框架在不同阶段发挥作用。

### 4.5.2 本项目采用的推理框架

当前 Agent 明确采用了以下三种推理框架：

1. `Plan-and-Execute`
2. `ReAct-style`
3. `Reflexion`

### 4.5.3 Plan-and-Execute 的落地方式

- 根据任务类型先生成计划步骤
- 明确每一轮任务的核心目标
- 为工具调用和回答生成提供结构化基础

### 4.5.4 ReAct-style 的落地方式

- 在执行阶段调用工具
- 构建 `Thought / Action / Observation` 轨迹
- 把工具观察引入问答流程

### 4.5.5 Reflexion 的落地方式

- 对当前结果做自检
- 判断是否需要补强
- 必要时触发重规划与二次修正

### 4.5.6 为什么说是“有机结合”而不是简单并列

本项目不是把三种框架做成互不相关的三块功能，而是把它们安排在同一条执行链路中：

- 先由 `Plan-and-Execute` 负责规划
- 再由 `ReAct-style` 负责行动与观察
- 再由 `Reflexion` 负责自检与修正
- 如果反思发现结果不足，还会回到计划层做补强

这说明三者之间是串联协同关系，而不是标签式堆砌。

### 4.5.7 当前达成程度说明

从课程加分项角度看，本项目已经：

- 明确引入了不止一种推理框架
- 在执行链路中给出了不同框架的职责分工
- 通过结构化数据把框架使用情况返回给前端展示

因此，这一项属于**已达成的核心加分点**。

同时也应客观看待：当前系统更偏向“多框架协同的课程实现版本”，后续若进一步增强动态规划、自治工具决策和多轮循环能力，将更能体现深层次的有机融合。

---

## 4.6 课程要求达成情况汇总

| 课程要求 | 本项目实现情况 | 达成结论 |
|---|---|---|
| 基本需求 1：展现规划和思考能力，而不是简单 workflow | 已实现规划、行动、反思、重规划、回答生成等多阶段链路 | 已达成 |
| 基本需求 2：至少设计实现 2 个主题相关外部工具 | 已实现 3 个主题相关工具，并纳入推理闭环 | 已达成 |
| 基本需求 3：具备自我修正能力，并能捕获系统异常 | 已实现 reflexion 修正与全局异常处理 | 已达成 |
| 基本需求 4：可视化内部状态流转情况 | 已通过 Web 工作台展示执行元信息、计划、轨迹、反思等 | 已达成 |
| 加分项：有机结合多种推理框架 | 已结合 Plan-and-Execute、ReAct-style、Reflexion | 已达成 |

---

## 5. 系统功能设计

### 5.1 前端功能

前端基于 `Vue 3 + Vite`，当前主要提供：

- 编程学习工作台
- 自然语言模式
- 结构化模式
- 快速提示词入口
- 历史记录侧栏
- 会话状态展示
- 用户登录 / 注册 / 登出
- 修改昵称 / 修改密码
- Markdown / 代码块 / 表格 / KaTeX 渲染
- 回答复制

### 5.2 后端功能

后端基于 `Spring Boot 3`，当前主要提供：

- AI 连通性测试
- 通用聊天接口
- Agent 结构化执行
- Agent 自然语言解析执行
- 工作台聚合能力
- 用户认证能力
- 历史记录与会话状态管理
- 数据持久化能力

### 5.3 支持的任务类型

当前支持以下任务类型：

- `GENERAL_CHAT`
- `ERROR_EXPLANATION`
- `CODE_REVIEW`
- `ALGORITHM_GUIDE`
- `PRACTICE_GENERATION`

---

## 6. 接口设计

本节综合 `接口文档.md`，对系统当前对外接口做统一说明。

### 6.1 接口分组

当前接口分为四类：

1. AI 模块
2. Agent 模块
3. 工作台模块
4. 认证模块

### 6.2 通用请求说明

#### 基础地址

```text
http://localhost:8080
```

#### 常用请求头

- `Content-Type: application/json`
- `Authorization: Bearer <token>`
- `X-Session-Id: session-xxx`

### 6.3 AI 模块接口

- `GET /api/ai/test`
- `POST /api/ai/chat`

用途：

- AI 服务联通性验证
- 通用模型聊天测试

### 6.4 Agent 模块接口

- `POST /api/agent/execute`
- `POST /api/agent/auto-execute`

用途：

- 结构化任务执行
- 自然语言自动解析执行

### 6.5 工作台模块接口

- `GET /api/workspace/dashboard`
- `POST /api/workspace/history`
- `GET /api/workspace/session-state`
- `POST /api/workspace/history/{id}/feedback`
- `POST /api/workspace/history/{id}/pin`

用途：

- 获取工作台概览
- 加载历史记录
- 获取当前会话状态
- 写入反馈
- 设置置顶

### 6.6 认证模块接口

- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `POST /api/auth/profile`
- `POST /api/auth/change-password`
- `POST /api/auth/logout`

用途：

- 注册
- 登录
- 获取当前用户
- 修改资料
- 修改密码
- 登出

---

## 7. 数据设计

### 7.1 数据表类型

系统当前主要包含以下数据：

- 用户数据 `app_user`
- 登录会话数据 `user_session`
- Agent 历史记录数据 `agent_conversation_history`

### 7.2 游客态与登录态隔离

#### 游客态

- 通过 `sessionId` 区分不同会话
- 历史记录仅归属于当前游客会话

#### 登录态

- 通过 `userId` 归属历史记录
- 登录 / 退出时切换前端 `sessionId`
- 避免游客历史与账号历史混淆

### 7.3 历史记录字段说明

历史记录主要包含：

- 用户 ID
- 会话 ID
- 请求类型
- 任务类型
- 用户问题
- 代码片段
- 回答摘要
- 最终回答
- 完整响应载荷
- 用户反馈
- 是否置顶
- 创建时间

---

## 8. 部署与 CI/CD 设计

### 8.1 部署对象

#### 前端

- `Vue 3 + Vite`
- 开发：`npm run dev`
- 生产：`npm run build`
- 最终由 `Nginx` 提供静态资源服务

#### 后端

- `Spring Boot`
- 通过 `Maven` 构建
- 产物为 `Jar`
- 可由 JRE 或 Docker 容器运行

#### 部署层

当前项目已提供：

- `deploy/docker-compose.yml`
- `deploy/nginx.conf`
- `scripts/deploy.sh`

### 8.2 环境划分建议

建议至少划分：

1. 开发环境
2. 测试环境
3. 生产环境

### 8.3 CI 阶段建议

#### 后端检查

```bash
cd backend
mvn clean test
```

#### 前端检查

```bash
cd frontend
npm ci
npm run build
```

### 8.4 构建产物建议

#### 方案 A：直接产出文件

- 前端：`frontend/dist/`
- 后端：`backend/target/*.jar`

#### 方案 B：产出 Docker 镜像

- 前端镜像：静态资源 + Nginx
- 后端镜像：Jar + JRE

### 8.5 CD 阶段建议

可通过以下方式部署：

```bash
./scripts/deploy.sh
```

或：

```bash
docker compose -f deploy/docker-compose.yml up -d
```

### 8.6 健康检查建议

部署后建议至少验证：

1. 首页可访问
2. 静态资源加载正常
3. `GET /api/ai/test` 正常返回
4. 用户可注册与登录
5. Agent 可正常执行
6. 历史记录可加载

### 8.7 回滚设计

建议每次构建保留：

- Git Commit SHA
- 时间戳
- Git Tag

一旦上线失败，可快速回滚到上一个稳定版本。

---

## 9. 项目亮点与后续扩展方向

### 9.1 当前亮点

- 具备完整前后端闭环
- 具备多阶段 Agent 执行链路
- 具备 3 个主题相关工具
- 具备内部状态可视化能力
- 具备用户系统和历史记录系统
- 具备容器化部署与 CI/CD 扩展基础

### 9.2 后续可扩展方向

1. 让模型动态生成更复杂的计划步骤
2. 增强工具调用的自治性与多轮循环能力
3. 引入更多工具，如联网搜索、代码执行、知识库检索
4. 引入 Swagger / OpenAPI 自动文档
5. 增加日志、监控、告警等可观测性能力
6. 打通 GitLab CI 或 GitHub Actions 实际流水线

---

## 10. 总结

本项目已经不是简单的“调用一个模型接口”，而是一个具备前端工作台、后端 Agent 编排、用户系统、历史记录、工具机制、状态可视化与部署方案的完整课程项目。

更重要的是，从课程要求角度看，本项目已经能够对以下内容给出明确落地说明：

- 为什么它不是简单 workflow
- 它有哪些主题相关外部工具
- 它如何实现自我修正与异常处理
- 它如何可视化内部状态流转
- 它如何有机结合多种推理框架

因此，这个项目不仅具备功能实现，也具备较好的课程答辩表达基础和后续工程扩展价值。
